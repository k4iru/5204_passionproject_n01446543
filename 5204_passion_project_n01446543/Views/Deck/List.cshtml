
@model IEnumerable<_5204_passion_project_n01446543.Models.DeckDto>

@{
    ViewBag.Title = "View";
}

<h2>View</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Team Name
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.DeckTitle)
            </td>

            <td>
                
                @Html.ActionLink("Details", "Details", new { id = item.DeckID }) |

            </td>
        </tr>
    }

</table>